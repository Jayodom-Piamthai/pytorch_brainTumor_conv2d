[project]
name = "tumor-scan-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.117.1",
    "uvicorn",
    "torch",
    "torchvision",
    "torchinfo>=1.8.0",
    "ultralytics",
    "pillow",
    "opencv-python >=4.10.0"
    # your other deps (torch, pillow, etc.)
]

[tool.vercel]
entrypoint = "webAPI:app"