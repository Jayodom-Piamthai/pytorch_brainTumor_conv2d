[project]
name = "tumor-scan-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.117.1",
    "torch",
    "torchvision",
    "ultralytics",
    "pillow",
    "opencv-python"
]

[tool.vercel]
entrypoint = "webAPI:app"

